# Default: install bear from the pinned tag on GitHub.
# To override with a local patched checkout, comment out the bear line below,
# then after `uv pip install -r requirements.txt` run:
#   uv pip install -e /mnt/c/Users/Scott/Documents/Work/bear-public-prep   # WSL
#   uv pip install -e C:/Users/Scott/Documents/Work/bear-public-prep       # Windows
bear @ git+https://github.com/snhwang/bear.git@v0.1.4
scipy
numpy
matplotlib
python-dotenv
openai
PyYAML
